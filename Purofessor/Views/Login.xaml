<Page x:Class="Purofessor.Views.Login"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Purofessor.Views"
      Background="Gray"
      mc:Ignorable="d" 
      d:DesignHeight="350" d:DesignWidth="500"
      Title="Login">

    <Grid>
        <Border Background="#CCFFFFFF" CornerRadius="10" Padding="20" 
            HorizontalAlignment="Center" VerticalAlignment="Center">
            <StackPanel Width="300">
                <TextBlock Text="Witaj w Purofessor!" 
                       FontSize="24" 
                       FontWeight="Bold" 
                       Foreground="Black" 
                       TextAlignment="Center" 
                       Margin="0,0,0,20"/>

                <TextBlock Text="Login:" FontWeight="SemiBold" />
                <TextBox x:Name="LoginTextBox" Margin="0,5,0,15" Height="30" VerticalContentAlignment="Center"/>

                <TextBlock Text="Hasło:" FontWeight="SemiBold"/>
                <PasswordBox x:Name="PasswordBox" Margin="0,5,0,20" Height="30" VerticalContentAlignment="Center"/>

                <Button Content="Zaloguj"
                    Height="35"
                    Background="DodgerBlue"
                    Foreground="White"
                    Click="LoginButton_Click"/>
            </StackPanel>
        </Border>
    </Grid>
</Page>
