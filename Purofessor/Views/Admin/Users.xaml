<Page x:Class="Purofessor.Views.Admin.Users"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      d:DesignHeight="450"
      d:DesignWidth="800"
      Title="Users">

    <Grid>
        <Border Style="{StaticResource MainBorderStyle}">
            <StackPanel Style="{StaticResource ContentStackPanelStyle}">

                <TextBlock Text="Lista użytkowników"
                           FontSize="24"
                           FontWeight="Bold"
                           Foreground="{DynamicResource ForegroundBrush}"
                           TextAlignment="Center"
                           Margin="0,0,0,20"/>

                <DataGrid Name="UsersDataGrid"
                          AutoGenerateColumns="False"
                          Background="{DynamicResource BackgroundBrush}"
                          Foreground="{DynamicResource ForegroundBrush}"
                          HeadersVisibility="Column"
                          GridLinesVisibility="None"
                          Margin="0,0,0,10"
                          RowBackground="{DynamicResource BackgroundBrush}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ID" Binding="{Binding id}" Width="Auto"/>
                        <DataGridTextColumn Header="Nick" Binding="{Binding name}" Width="*" />
                        <DataGridTextColumn Header="Email" Binding="{Binding email}" Width="*" />
                        <DataGridTextColumn Header="Email verified" Binding="{Binding emailverifiedat}" Width="Auto" />
                        <DataGridTextColumn Header="Utworzono" Binding="{Binding createdat}" Width="Auto" />
                        <DataGridTextColumn Header="Zaktualizowano" Binding="{Binding updatedat}" Width="Auto" />
                        <DataGridCheckBoxColumn Header="Admin" Binding="{Binding is_admin}" Width="Auto" />
                        <DataGridTemplateColumn Header="Akcje" Width="Auto">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="Edytuj"
                                            Click="EditUser_Click"
                                            Padding="5,2"
                                            Tag="{Binding}" />
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>

            </StackPanel>
        </Border>
    </Grid>
</Page>
