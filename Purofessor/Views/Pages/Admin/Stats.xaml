<Page x:Class="Purofessor.Views.Pages.Admin.Stats"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Stats">

    <Grid>
        <Border Style="{StaticResource MainBorderStyle}">
            <Grid Margin="20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <TextBlock Text="Statystyki wyszukiwań championów"
                           FontSize="20"
                           FontWeight="Bold"
                           Foreground="{DynamicResource ForegroundBrush}"
                           HorizontalAlignment="Center"
                           Margin="0,0,0,20" />

                <DataGrid Name="StatsDataGrid"
                          IsReadOnly="True"
                          Grid.Row="1"
                          AutoGenerateColumns="False"
                          Background="{DynamicResource BackgroundBrush}"
                          Foreground="{DynamicResource ForegroundBrush}"
                          BorderBrush="{StaticResource PrimaryBrush}"
                          BorderThickness="1"
                          HeadersVisibility="Column"
                          GridLinesVisibility="None"
                          RowBackground="{DynamicResource BackgroundBrush}"
                          AlternatingRowBackground="{DynamicResource BackgroundBrush}"
                          FontSize="14"
                          FontWeight="SemiBold"
                          RowHeight="35"
                          ColumnHeaderHeight="30"
                          ScrollViewer.VerticalScrollBarVisibility="Auto"
                          Margin="0,0,0,10">

                    <DataGrid.Resources>
                        <!-- Styl nagłówków -->
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="{StaticResource GradientStartBrush}" />
                            <Setter Property="Foreground" Value="White"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                            <Setter Property="HorizontalContentAlignment" Value="Center"/>
                            <Setter Property="BorderBrush" Value="{StaticResource PrimaryBrush}" />
                            <Setter Property="BorderThickness" Value="0,0,0,1" />
                            <Setter Property="Padding" Value="5,3"/>
                        </Style>

                        <!-- Styl wierszy -->
                        <Style TargetType="DataGridRow">
                            <Setter Property="Background" Value="{StaticResource BackgroundBrush}" />
                            <Setter Property="Foreground" Value="{StaticResource ForegroundBrush}" />
                            <Setter Property="BorderBrush" Value="{StaticResource PrimaryBrush}" />
                            <Setter Property="BorderThickness" Value="0,0,0,1" />
                            <Style.Triggers>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Background" Value="{StaticResource GradientEndBrush}" />
                                    <Setter Property="Foreground" Value="White" />
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </DataGrid.Resources>

                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Champion" Binding="{Binding Champion.Name}" Width="*" />
                        <DataGridTextColumn Header="Liczba wyszukiwań" Binding="{Binding Total}" Width="200"
                                            ElementStyle="{StaticResource RightAlignedCellStyle}" />
                    </DataGrid.Columns>
                </DataGrid>

            </Grid>
        </Border>
    </Grid>
</Page>
